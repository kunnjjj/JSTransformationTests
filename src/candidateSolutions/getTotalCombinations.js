/**
 *
 * @param
 * featuresList: {featureID:string; values:string[]}[]
 *
 * @returns number
 */
const getTotalCombinations = (featuresList) => {
  // COMPLETE THIS FUNCTION
};

module.exports = getTotalCombinations;
